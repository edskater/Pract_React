     version: '3'
     services:
       app:
         build:
           context: .
           dockerfile: Dockerfile
         ports:
           - '5000:5000'
         environment:
           - DATABASE_URL=postgres://username:password@host:port/database_name
    
